# RENDER ENVIRONMENT VARIABLES
# Copy and paste these into Render dashboard:

NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/finance-dashboard?retryWrites=true&w=majority
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-min-32-characters
JWT_EXPIRE=7d
CORS_ORIGIN=https://finance-dashboard-api.onrender.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# IMPORTANT:
# 1. Replace username:password with your MongoDB Atlas credentials
# 2. Replace cluster.mongodb.net with your actual cluster URL
# 3. Generate a secure JWT_SECRET (min 32 characters)
# 4. Update CORS_ORIGIN with your actual Render URL after deployment
